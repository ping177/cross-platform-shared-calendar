# v0.1.9 Shared Tasks MVP Specification

Status: `SCOPE FROZEN / IMPLEMENTATION NOT STARTED`

Verdict: `V019_SHARED_TASKS_SCOPE_FROZEN`

Multi-space decision: `MULTISPACE_NOT_REQUIRED_FOR_V019`

## Objective

v0.1.9 adds the smallest useful shared household Task capability while preserving the existing Calendar and Reminder domains.

- An Event represents when something happens.
- A Task represents something that remains to be completed.
- Every Task belongs to exactly one Space.
- Current Space members collaborate on the same Task set through Supabase persistence, RLS, and Realtime.

Success means two authenticated members can create, view, assign, edit, complete, reopen, and delete Tasks with cross-device Realtime synchronization. This specification does not authorize implementation, Production changes, or deployment.

## Existing Architecture Context

- Frontend: React 18, TypeScript, Vite, Tailwind.
- Backend: Supabase Auth, PostgreSQL, RLS, PostgREST, and Realtime.
- Canonical business persistence remains Supabase; no filesystem or browser-local Task database is introduced.
- The current product enforces one Space per user and at most two members per Space.
- Existing membership and RLS helpers are already based on explicit `space_id` values.
- Existing Event Realtime uses a Space-filtered `postgres_changes` subscription and canonical reload after changes.

## Commands

These commands describe the existing project workflow. This docs-only freeze requires only the documentation checks listed at the end of this specification.

```bash
npm run dev
npm run build
node --test tests/*.test.ts tests/*.test.js
git diff --check
```

The development server must use `http://127.0.0.1:5175` with the repository's fixed strict port.

## Project Structure

- `src/`: application source and future Task frontend integration.
- `tests/`: Node behavior and source-contract tests.
- `supabase/schema.sql`: fresh-install canonical schema; not an existing-environment migration.
- `supabase/patches/`: reviewed incremental database patches.
- `supabase/tests/`: pgTAP database contract and authorization tests.
- `docs/`: product, architecture, testing, governance, and status documentation.

## Frozen Product Semantics

### Creator and ownership

- `created_by` records creator attribution only.
- The creator receives no exclusive read, edit, assignment, completion, reopen, or delete permission.
- `created_by` is immutable after creation.

### Assignment

- `assigned_to_user_id = <current member id>` means that member is responsible.
- `assigned_to_user_id = null` means shared responsibility.
- Assignment is not an access-control boundary.
- A non-null assignee must be a current member of the same Space as the Task.
- Any current Space member may assign, reassign, or change a Task to shared.
- If an assigned member leaves the Space, the Task automatically becomes shared by setting `assigned_to_user_id` to null.
- The member-leave behavior is frozen, but its SQL mechanism is not. Slice 1 must choose the smallest target-compatible database FK action or narrowly scoped trigger after verifying the actual PostgreSQL/Supabase capability.

### Completion

- Status is exactly `open` or `completed`.
- New Tasks default to `open`.
- Any current Space member may complete or reopen a Task.
- v0.1.9 does not store `completed_at`, `completed_by`, or completion history.

### Due date

- `due_on` is optional.
- It is a PostgreSQL `date`, with no time or timezone.
- It does not create, update, or duplicate a Calendar Event.

### Scope

- A Task has an explicit `space_id`.
- A Task does not have Event-style `scope` or `owner_user_id` fields.
- Space membership controls visibility; assignment expresses responsibility.

## Frozen Data Contract

v0.1.9 adds exactly one product table: `public.tasks`.

| Field | Contract |
| --- | --- |
| `id` | Generated UUID primary key |
| `space_id` | Required reference to the existing Space; immutable after creation |
| `created_by` | Required authenticated creator attribution; immutable after creation |
| `assigned_to_user_id` | Nullable; null means shared; non-null must be a current member of the same Space |
| `title` | Required canonical value; trimmed length 1–200 characters and no leading/trailing whitespace |
| `status` | Required `open` or `completed`; defaults to `open` |
| `due_on` | Nullable PostgreSQL `date` |
| `created_at` | Required timestamp using the existing project convention |
| `updated_at` | Required timestamp using the existing touch pattern |

Database invariants are authoritative. The frontend may trim and validate for usability, but it is not the final security boundary. Slice 1 chooses the simplest deterministic title enforcement that satisfies the stored-value contract without adding a validation framework.

The table must also receive:

- one reasonable list-oriented index;
- minimal immutable-identity enforcement for `space_id` and `created_by`;
- same-Space current-member assignment validation;
- conventional authenticated CRUD grants and RLS policies;
- inclusion in the existing Supabase Realtime publication;
- the existing compatible replica-identity pattern needed for Space-filtered delete delivery.

The exact index column order and member-leave SQL mechanism remain Slice 1 implementation decisions because they require verification against the actual query and target database. The frozen behavior does not depend on either choice.

## Permission Model

| Operation | Frozen permission |
| --- | --- |
| Create | Any current member of the target Space; `created_by` must equal the authenticated user |
| View | Every current member of the Task's Space |
| Edit title or due date | Every current member of the Task's Space |
| Change assignment | Every current member; target must be a current same-Space member or null |
| Complete or reopen | Every current member |
| Delete | Every current member; UI requires confirmation |
| Former or non-member access | None |
| Creator-specific privilege | None |
| Assignee-exclusive privilege | None |

RLS, database constraints, membership validation, and immutable identity rules are the authorization and integrity boundary. Frontend visibility or disabled controls are UX only.

Ordinary Task CRUD should use direct Supabase/PostgREST operations. Do not add a Task CRUD RPC unless Slice 1 demonstrates a concrete atomicity requirement that cannot be met safely by the conventional table path.

## Security and Abuse Cases

Slice 1 must prove the following at the database boundary:

- a non-member cannot read or mutate Tasks;
- a member cannot create a Task in a Space they do not belong to;
- a client cannot forge `created_by` for another user;
- a client cannot move an existing Task to another Space or change its creator;
- an assignee cannot be selected from another Space or from former members;
- title values that are blank, edge-padded, or longer than 200 characters are rejected as canonical stored values;
- invalid status values are rejected;
- Realtime visibility does not broaden beyond RLS-authorized Space data.

Task titles remain plain text rendered through React's normal escaping. v0.1.9 adds no HTML rendering, external URL fetch, file input, secret, or new authentication flow.

## Realtime Model

- Reuse Supabase Realtime; do not introduce another synchronization architecture.
- Subscribe to Task INSERT, UPDATE, and DELETE changes filtered by the explicit current/selected `space.id`.
- Reload the canonical Task list for that Space after a change, matching the existing Event pattern.
- A create on device A appears on device B without refresh.
- Assignment, title, due-date, completion, and reopen changes synchronize.
- Deletion synchronizes through the existing compatible filtered-delete replica-identity pattern.

## Calendar Boundary

- `tasks.due_on` is Task metadata only.
- v0.1.9 does not copy a Task into `events`, create an Event, update an Event, participate in Event recurrence, or introduce dual persistence.
- A future version may design a read-only Calendar projection of due Tasks without duplicating persistence. That work is not part of v0.1.9.

## Reminder Boundary

v0.1.9 does not change or reuse:

- `events.reminder_kind`;
- `send-reminders`;
- `reminder_deliveries`;
- Supabase Cron or `pg_net` scheduling;
- Web Push delivery or recipient resolution;
- ordinary or recurring Event Reminder projection.

Task Reminder is deferred. The `CLOSED / PASS` v0.1.8 Reminder architecture stays closed.

## Multi-space Decision

Decision: `MULTISPACE_NOT_REQUIRED_FOR_V019`.

Evidence:

- Task identity explicitly includes `space_id` from the first version.
- Existing membership and RLS helpers accept explicit Space IDs.
- Realtime can filter by an explicit current/selected Space ID.
- Current one-Space assumptions are concentrated in the unique membership index, create/join lifecycle, and frontend current-Space selection.
- A later Multi-space version can change membership lifecycle and add a Space selector without changing Task identity or assignment semantics.

Future-compatibility requirements:

- Task implementation receives and uses the explicit current/selected `space.id`.
- It never derives a Space implicitly from user identity.
- It does not add another one-space-per-user constraint.
- It never omits `space_id` from Task persistence or Realtime filtering.

v0.1.9 does not implement Multi-space, a Space switcher, multi-space onboarding, or broad membership/RLS rewrites.

## Complexity Budget

The approved budget is:

- one new product table: `tasks`;
- one reasonable list-oriented index;
- bounded constraints and assignment validation;
- minimal immutable-identity enforcement;
- conventional RLS CRUD policies and grants;
- direct Supabase CRUD;
- the existing Realtime pattern;
- no new dependency unless implementation proves it necessary and receives approval.

Do not add an Edge Function, Cron job, queue, workflow engine, local persistence, external storage, generic activity log, or another synchronization layer.

## Minimal UI Scope

Add a compact `Calendar / Tasks` module switch without refactoring the existing Calendar shell, replacing routing, creating a Design System, or redesigning Calendar behavior.

The Tasks module contains:

- open Tasks as the primary section;
- completed Tasks as a secondary section so they remain available to reopen;
- create and edit Task flows;
- delete with confirmation;
- complete and reopen controls;
- title, assignment, and optional due date;
- labels from actual current Space members plus a shared option.

For a one-member Space, assignment shows only the current member and shared. It must not invent a partner.

Open Tasks sort by `due_on` ascending with nulls last, then `created_at` ascending, then `id` ascending. This uses persisted fields as a deterministic tie-break and does not introduce a custom ordering column or framework.

## Implementation Slices

Implementation remains sequential and requires explicit approval after this docs freeze review.

### Slice 1 — Task persistence and authorization

**Description:** Add the additive database foundation and prove its authorization and integrity contracts. No Task UI is included.

**Acceptance criteria:**

- The `tasks` table, constraints, assignment/member-leave behavior, immutable identity, RLS, grants, Realtime publication, replica identity, and one list index match this specification.
- Authorized members can perform the frozen operations; former/non-members and cross-Space assignment attempts are rejected.
- The fresh schema and one reviewed incremental patch represent the same final Task contract.

**Verification:**

- Focused pgTAP database tests pass.
- Existing database regressions and relevant schema-contract tests pass.
- `git diff --check` passes and no UI, Reminder, deployment, or dependency change is included.

**Dependencies:** Approved docs freeze and separate Slice 1 implementation authorization.

### Slice 2 — Minimal CRUD, UI, and Realtime

**Description:** Add the frontend Task model and minimal Tasks module on top of the accepted Slice 1 contract.

**Acceptance criteria:**

- Current members can list, create, edit, assign, complete, reopen, and delete Tasks through the minimal UI.
- Grouping and ordering are deterministic, one-member assignment is truthful, and Calendar behavior remains unchanged.
- Two authenticated sessions observe Space-filtered create/update/delete synchronization without refresh.

**Verification:**

- Focused Node tests and the complete relevant frontend regression suite pass.
- `npm run build` passes.
- Authenticated two-session desktop and narrow mobile smoke pass at `http://127.0.0.1:5175`.

**Dependencies:** Accepted Slice 1 database contract and separate Slice 2 authorization.

### Slice 3 — Production acceptance and closeout

**Description:** Roll out only the reviewed database/frontend artifacts and close the version after bounded Production acceptance.

**Acceptance criteria:**

- The reviewed patch and compatible frontend are deployed only after separate approval.
- Production two-session CRUD, RLS isolation, Realtime, persistence, and supported mobile layout pass.
- Canonical docs accurately record deployment and acceptance status.

**Verification:**

- Production postflight proves the expected Task schema/RLS/publication without unrelated changes.
- Two authenticated Production sessions pass create/edit/assign/complete/reopen/delete synchronization.
- Only concrete defects found by acceptance are corrected; polish is not automatically added.

**Dependencies:** Accepted Slice 2 and separate Production/deployment authorization.

## Overall Acceptance Criteria

v0.1.9 is complete only when:

- the frozen Task data and permission contracts are enforced by the database;
- the minimal UI supports every approved Task operation without expanding scope;
- two current members receive correct Space-filtered Realtime updates;
- non-members and cross-Space assignments remain blocked;
- member departure converts affected assignments to shared;
- Calendar and Reminder behavior regressions are absent;
- local build/tests and separately authorized Production acceptance pass;
- canonical status, testing, decision, backlog, README, and development-log documentation is current.

## Deferred Scope

- Task Reminder or Push delivery.
- Recurring Tasks.
- Subtasks.
- Priority.
- Tags or categories.
- Shared Lists or shopping/list semantics.
- Description, comments, attachments, or activity history.
- `completed_at`, `completed_by`, or completion history.
- Search or a general filter framework.
- Analytics.
- Drag/custom ordering or an ordering framework.
- Automatic Event creation or Task/Event dual persistence.
- Multi-space implementation or Space switching.
- UI/UX overhaul or a new Design System.

The directional roadmap remains v0.1.10 Shared Lists, v0.1.11 Important Dates / Anniversaries, and v0.1.12 Tags / Color = Who. Those versions are not frozen by this specification.

## Risks

- Collaborative permissions intentionally allow any current member to edit or delete any Task; this is household collaboration, not enterprise RBAC.
- Status-only completion has no actor/time audit trail. That is an explicit MVP tradeoff.
- Date-only due values cannot express a time-of-day deadline. Adding time would change the product boundary and is deferred.
- No member-removal UI currently exists, so member-leave behavior must still be tested directly at the database boundary in Slice 1.
- The target database must be checked before choosing the smallest assignment-clearing mechanism; this specification freezes the behavior, not unsupported SQL syntax.
- The current frontend has a single-Space shell and a large `App.tsx`; Slice 2 may extract a focused Task component, but it must not turn that need into a shell or routing rewrite.

## Boundaries

### Always

- Use explicit `space_id`, database-enforced membership, RLS, and deterministic validation.
- Keep migrations additive and update both the incremental patch and fresh-install schema.
- Use temporary or injected test data/paths and preserve Supabase as canonical persistence.
- Run the smallest relevant checks plus full regressions proportionate to each slice.

### Ask first

- Begin any implementation slice.
- Add a dependency, RPC, Edge Function, deployment, Production mutation, or scope item not frozen here.
- Change authentication, Space lifecycle, member capacity, Multi-space behavior, Event semantics, or Reminder architecture.

### Never in v0.1.9

- Treat assignment as authorization.
- Rely on frontend validation or hidden controls as the permission boundary.
- Infer a Task's Space from the user instead of persisting explicit `space_id`.
- Create Task/Event dual persistence or silently enable Task reminders.
- Store Task business data in repository-local or filesystem persistence.

## Scope-Freeze Verification

This docs-only freeze is verified with:

```bash
git diff --check
git status --short
git diff --stat
```

No application build, database test, deployment, Production operation, or implementation work is required for the scope-freeze commit.
